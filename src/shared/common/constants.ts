export const passwordRegex = '^(?=.*d)(?=.*[a-zA-Z]).{8,16}$'

export const phoneNumberRegex = '^\\d{10}$' //matches a line that contains exactly 10 digits and nothing else(without any hyphens, spaces, or other punctuation).
